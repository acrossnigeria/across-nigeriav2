import Layout from "@/components/Layout"

export default function Notifications() {
    return (
        <Layout>
            <div className="h-screen w-full bg-gray-100 flex flex-row justify-center pt-[50px]">
                <span className="text-[14px] text-gray-600">No notifications</span>
            </div>
        </Layout>
    )
}